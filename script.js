var students = ['Forogoh', 'Sohrab', 'Elham'];
var teachers = [ 'Zabe', 'Javeed'];
var totalTeachers = [];
var result = 0;
   for (var i = 0; i < students.length; i++) {
      for (var j = 0; j < teachers.length; j++) {
         totalTeachers[result] = students[i] + teachers[j];
          console.table("total length is");
result++
}
}